<%= form_for(@tower) do |f| %>
  <% if @tower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tower.errors.count, "error") %> prohibited this tower from being saved:</h2>

      <ul>
      <% @tower.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "اسم البرج" %><br />
    <%= f.text_field :name %>
  </div>

  <div id="partners" >
    <h1>الشركاء</h1>
    <a href="#" class="add">اضافة شريك</a>
    <div class="items">
      <%= f.nested_fields_for :partners do |builder| %>
        <%= render 'partner_fields', f: builder %>
      <% end %>
    </div>

  </div>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>